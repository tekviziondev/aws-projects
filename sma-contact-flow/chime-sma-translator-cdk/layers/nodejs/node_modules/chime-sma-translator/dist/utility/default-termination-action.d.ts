import { ChimeActions } from "../const/chime-action-types";
export declare class TerminatingFlowUtil {
    /**
      * This Terminates the existing call if there are any error occured in the Flow execution
      * @param smaEvent
      * @param actionType
      * @returns SMA Error Speak Action and Hang UP action
      */
    terminatingFlowAction(smaEvent: any, actionType: string): Promise<{
        SchemaVersion: string;
        Actions: ({
            Type: ChimeActions;
            Parameters: {
                Engine: string;
                CallId: any;
                Text: string;
                TextType: string;
                LanguageCode: string;
                VoiceId: string;
            };
        } | {
            Type: ChimeActions;
            Parameters: {
                SipResponseCode: string;
                CallId: string;
            };
        })[];
    }>;
}
