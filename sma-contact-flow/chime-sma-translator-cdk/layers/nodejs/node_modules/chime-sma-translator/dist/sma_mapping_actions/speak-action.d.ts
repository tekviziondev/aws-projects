import { ChimeActions } from "../const/chime-action-types";
import { IContextStore } from "../const/context-store";
import { SpeechParameter } from "./speech-parameter";
/**
  * Making the SMA action for converting the Text or SSML to perform speak action.
  * @param smaEvent
  * @param action
  * @param contextStore
  * @returns SMA action
  */
export declare class SpeakAction extends SpeechParameter {
    execute(smaEvent: any, action: any, contextStore: IContextStore): Promise<{
        SchemaVersion: string;
        Actions: ({
            Type: ChimeActions;
            Parameters: {
                Engine: string;
                CallId: any;
                Text: string;
                TextType: string;
                LanguageCode: string;
                VoiceId: string;
            };
        } | {
            Type: ChimeActions;
            Parameters: {
                SipResponseCode: string;
                CallId: string;
            };
        })[];
    } | {
        SchemaVersion: string;
        Actions: any[];
        TransactionAttributes: {
            [x: string]: any;
        };
    }>;
}
