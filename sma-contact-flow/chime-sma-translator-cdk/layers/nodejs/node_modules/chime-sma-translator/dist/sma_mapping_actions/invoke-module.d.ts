import { IContextStore } from "../const/context-store";
/**
  * Invoke a Module from the existing contact flow, to perform a particular task
  * @param smaEvent
  * @param action
  * @param amazonConnectInstanceID
  * @param bucketName
  * @param contextStore
  * @returns SMA action defined in the Module
  */
export declare class InvokeModule {
    processFlowActionInvokeFlowModule(smaEvent: any, action: any, amazonConnectInstanceID: string, bucketName: string, contextStore: IContextStore): Promise<any>;
}
