import { IContextStore } from "../const/context-store";
/**
  * Making a SMA action to perform Repeats the looping action block for the specified number of times. After which, the loop complete block will be executed
  * @param smaEvent
  * @param action
  * @param actions
  * @param amazonConnectInstanceID
  * @param bucketName
  * @param contextStore
  * @returns SMA action
  */
export declare class Loop {
    processFlowActionLoop(smaEvent: any, action: any, actions: any, amazonConnectInstanceID: string, bucketName: string, contextStore: IContextStore): any;
}
