import { IContextStore } from "../const/context-store";
/**
  * Ends the execution of the current Module and return Back to Orginal Contact flow.
  * @param smaEvent
  * @param amazonConnectInstanceID
  * @param bucketName
  * @param contextStore
  * @returns orginal Contact Flow next action defined after end flow Module block
  */
export declare class EndModule {
    processFlowActionEndFlowModuleExecution(smaEvent: any, amazonConnectInstanceID: string, bucketName: string, contextStore: IContextStore): any;
}
