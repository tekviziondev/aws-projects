import { IContextStore } from "../const/context-store";
/**
  * Comparing Contact Attributes with the operator specified by the user and navigating to the Next Contact Flow Action based on the result of comparison
  * @param smaEvent
  * @param action
  * @param actions
  * @param amazonConnectInstanceID
  * @param bucketName
  * @param contextStore
  * @returns SMA action
  */
export declare class CompareAttribute {
    processFlowActionCompareContactAttributes(smaEvent: any, action: any, actions: any, amazonConnectInstanceID: string, bucketName: string, contextStore: IContextStore): any;
}
