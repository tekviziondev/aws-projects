import { IContextStore } from "../const/context-store";
/**
  * Making a SMA action to perform Hang up the Active Call.
  * @param smaEvent
  * @param contextStore
  * @returns SMA action
  */
export declare class DisconnectParticipant {
    processFlowActionDisconnectParticipant(smaEvent: any, contextStore: IContextStore): Promise<{
        SchemaVersion: string;
        Actions: any[];
    }>;
}
