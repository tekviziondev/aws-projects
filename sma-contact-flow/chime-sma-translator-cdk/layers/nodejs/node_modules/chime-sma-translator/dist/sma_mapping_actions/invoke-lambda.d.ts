import { IContextStore } from "../const/context-store";
/**
  * Invokes the External Lambda Function and stores the result in the ContextStore
  * @param smaEvent
  * @param action
  * @param actions
  * @param amazonConnectInstanceID
  * @param bucketName
  * @param contextStore
  * @returns The Next SMA action to perform
  */
export declare class InvokeLambda {
    processFlowActionInvokeLambdaFunction(smaEvent: any, action: any, actions: any, amazonConnectInstanceID: string, bucketName: string, contextStore: IContextStore): any;
}
